import "./globals.css"

// This is the root layout that just exports children
// The main layout logic is in app/[locale]/layout.jsx
export default function RootLayout({ children }) {
  return children;
}